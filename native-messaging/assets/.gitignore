# Per-user generated artifacts — never commit these.
# The private key pins the extension ID; sharing it allows impersonation.
scripts/extension_key.pem
host/extension_id.txt

# Host manifest generated by install_host.bat
host/*.json

# Host error log (see troubleshooting)
host/host.err.log
